# Gemini CLI 辅助研发编码：Skills 实战指南

本教程以你的 `python-robotics-sim` 项目为例，介绍如何高效利用 Gemini CLI（及其底层的 Skills/专家模型）来辅助研发写代码。

**核心思想：** 不要把大段代码直接粘贴到聊天框中！Gemini CLI 具备读取你本地工作区的能力。你应该像指导一位“能看到你代码库的资深同事”一样与它沟通。

---

## 🔁 标准协作工作流 (The Method)

高效使用 AI 辅助编码，通常遵循以下四步：

### 1. 明确目标与提供路径 (指路而非贴代码)
告诉系统你需要做什么，并精确指出相关文件的路径。AI 会自动去读取和分析。

> **示例 Prompt：**
> “请阅读 `tools/robot_client.py` 中的 `ArisRobotClient` 类，我想在其中增加一个发送心跳包（Heartbeat）的方法。”

### 2. 按需调用特定 Skill (如设计文档或自建规范)
如果你要开发一个复杂的模块（比如新增一个视觉处理流程），不要直接让 AI 怼代码。你可以先调用内置的 `generate-tdd` (技术设计文档) Skill。

> **示例 Prompt：**
> “我想在 `KAANH_Digital_Twin/logic` 下新增一个传送带速度预估模块。请先使用 `generate-tdd` 帮我输出一份技术设计和接口定义，不要直接写代码。”

*(系统会自动激活对应的 Skill，遵循标准的软件工程设计流程为你出具方案)*

### 3. 指令式编码 (Directives)
当设计清晰或任务简单时，下达明确的编码指令。提出具体的约束（如：错误处理、风格规范）。

> **示例 Prompt：**
> “请在 `tools/robot_client.py` 中实现刚才规划的 `send_heartbeat` 方法。
> **要求**：
> 1. 复用现有的 `_send_raw_command` 方法。
> 2. 如果 WebSocket 断开，自动尝试重连（调用已有的 `connect` 方法）。
> 3. 按照项目中 `[异常]` 和 `[连接]` 的打印风格添加日志。”

### 4. 验证与回溯 (Validate & Rewind)
AI 编写并自动修改文件后，你可以在编辑器（如 VS Code）中查看变更：
*   **如果不满意：** 直接在终端输入 `/rewind`（或连按两次 Esc），选择“撤销代码并回退对话”，让它重新写。
*   **如果部分正确：** 说出具体的修改意见，例如：“逻辑没问题，但在第 45 行附近，请加上超时的异常捕获”。

---

## 💡 本地项目实战案例

假设我们需要在 `robot_client.py` 里添加批量指令发送功能，以下是推荐的对话流程：

**👨‍💻 研发人员 (你):**
> “请查看 `tools/robot_client.py`。目前的 `_send_raw_command` 只能发单条指令。请帮我写一个 `send_batch_commands` 方法，支持传入指令列表，并在每条指令之间默认间隔 0.1 秒。”

**🤖 Gemini CLI:**
> *(内部动作：调用 `read_file` 读取 `robot_client.py`，分析现有的 ws 连接逻辑和 `_send_raw_command` 的签名)*
> *(内部动作：调用 `replace` 将新写好的方法精准插入到 `ArisRobotClient` 类的末尾)*
> **回复:** “已在 `robot_client.py` 中添加了 `send_batch_commands` 方法，支持遍历列表并加入延时机制。你可以检查一下。”

*(注意：整个过程你没有复制一行代码，AI 也没有在聊天框里打印大段代码，它是直接修改了你的文件。)*

---

## ⚠️ 高效避坑指南

1.  **控制代码生成范围**：如果你怕 AI 把整个文件重写搞乱，加上一句约束：“仅在文件末尾添加新方法，不要修改已有逻辑”。
2.  **让 AI 先跑测试**：如果你写了单元测试（如 `test/test_latency.py`），可以在修改代码后直接让 AI 运行测试：“修改完成后，请帮我执行 `pytest test/test_latency.py` 验证一下”。
3.  **定制你的专属 Skill**：如果你的团队有非常严格的代码规范（例如特定的单例模式写法、特有的版权声明），你可以对系统说：“我想创建一个新的 Skill，专门用于指导生成符合我们公司规范的 ROS 节点代码”，AI 会调用 `skill-creator` 引导你建立规范。
